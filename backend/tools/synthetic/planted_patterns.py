"""Planted patterns P1-P7 (see README). Plant labels are written to a separate
answer-key file for tests only; the pipeline never reads them.

TODO: one function per pattern that mutates the generated event stream.
"""
