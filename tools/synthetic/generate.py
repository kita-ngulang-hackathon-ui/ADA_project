"""Synthetic dataset generator CLI (see tools/synthetic/README.md).

TODO:
- Parse SYNTHETIC_* settings; seed random.Random(SYNTHETIC_SEED) (never global random).
- Build tenants/users/edges/events/historical campaigns; apply planted patterns.
- Write fixtures deterministically (sorted keys, stable ordering).
"""


def main() -> None:
    raise NotImplementedError


if __name__ == "__main__":
    main()
