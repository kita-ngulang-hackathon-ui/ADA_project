// Typed client for the console API (/console/v1).
// TODO:
// - apiFetch<T>(path, init): base URL from NEXT_PUBLIC_API_BASE_URL, credentials: "include".
// - Parse error envelope {error: {code, message, details, request_id}} into ApiError.
// - Functions per endpoint: listUsers, getUserRisk, runPipeline, runAllocation,
//   listRecommendations, approveRecommendation, rejectRecommendation, getMeasurement, getFeedbackSummary.
export {};
