# components

Shared UI components (shadcn/ui based). Planned:

- `SyntheticDataBanner` — always visible where numbers appear (§4 honesty rule).
- `StatusBadge` — recommendation status colors.
- `PatternBadge` — `CIRCLE_SPECIFIC` / `MARKET_DRIVEN` / `STABLE`.
- `SegmentBadge` — `PERSUADABLE` / `SURE_THING` / `LOST_CAUSE` / `SLEEPING_DOG`.
- `StabilityChart` — neighbor activity ratio now vs 30 days ago (Recharts).
- `LiftChart` — retention by arm.
- `ReasonList` — reason factors with narration source label (LLM / TEMPLATE).
- `ApprovalPanel` — approve / reject with note; disabled unless a reviewer session exists.
