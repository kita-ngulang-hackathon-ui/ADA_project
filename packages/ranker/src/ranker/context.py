"""Ranker in-context rows from labeled examples (requirements 7, 11).

TODO: build X (fixed column order) and y (realized incremental value), single tenant.
"""

RANKER_COLUMNS: list[str] = []  # TODO


def build_context(labeled_examples):
    raise NotImplementedError
